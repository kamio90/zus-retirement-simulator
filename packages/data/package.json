{
  "name": "@data",
  "version": "1.0.0",
  "description": "Data layer - JSON snapshots from ZUS XLSX sources",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
