{
  "name": "@zus/data",
  "version": "1.0.0",
  "description": "Data layer - JSON snapshots from ZUS XLSX sources",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/json dist/json",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
